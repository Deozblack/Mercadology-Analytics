import { Identifiers } from "@angular/compiler";

export class RegistroModel{
    id: string;
    nombre: string; 
    apellido: string;
    puesto?: string;
    rol?: string;
    depto?: string;
    correo?: string; 
    password?: string;
}

